@using my_website.Models.Entity
@model List<Tbl_Accounts>

@{
    ViewBag.Title = "Accounts";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="p-5">
    <div class="float-left mb-3">
        <a href="/Admin/AddAccount" class="btn btn-info">Add Account</a>
    </div>
    <table class="table table-borderless table-responsive-sm table-responsive-md bg-light">
        <thead align="center">
            <tr>
                <th>Name</th>
                <th>Icon</th>
                <th>Link</th>
                <th colspan="2">Operations</th>
            </tr>
        </thead>
        @foreach (var x in Model)
        {
            <tbody align="center">
                <tr>
                    <td>@x.NAME</td>
                    <td>@x.Tbl_AccountIcons.ICON</td>
                    <td>@x.LINK</td>
                    <td><a href="/Admin/AccountUpdate/@x.ID" class="btn btn-outline-success">Update</a></td>
                    <td><a href="/Admin/AccountDelete/@x.ID" class="btn btn-outline-danger">Delete</a></td>
                </tr>
            </tbody>
        }
    </table>
</div>
<div style="height: 350px"></div>
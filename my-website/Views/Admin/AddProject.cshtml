@model my_website.Models.Entity.Tbl_Projects

@{
    ViewBag.Title = "AddProject";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<form method="post">
    <div class="p-5">
        <div class="mb-3">
            <div class="row">
                <div class="form-group col-6">
                    <label class="form-label">Header</label>
                    <input type="text" name="HEADER" class="form-control" placeholder="Project Header.." />
                </div>
                <div class="form-group col-6">
                    <label class="form-label">Category</label>
                    @Html.DropDownListFor(x => x.Tbl_ProjectCategories.ID, (List<SelectListItem>)ViewBag.Category, new { @class = "form-control" })
                </div>
                <div class="form-group col-12">
                    <label class="form-label">Image</label>
                    <input type="text" name="IMAGE" class="form-control" placeholder="Link.." />
                </div>
            </div>
            <div>
                <button class="btn btn-primary btn-lg btn-block">Add</button>
            </div>
        </div>
    </div>
</form>
<div style="height: 170px;"></div>